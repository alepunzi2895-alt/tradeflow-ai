# TradeFlow AI — Known Issues & Backlog

## Bug Aperti

| Severity | File | Issue |
|---|---|---|
| 🤖 AI-Flagged 2026-07-09 | `scripts/mt5-bot.py` | **S00_MFKK** TRADE_SILENCE: Il pattern è identico al bug storico documentato: signals.py produce segnali 'puri' sani (gate interni su ADX/DI/soglie sono espliciti e testati nel backtest), ma il silenzio di 34gg coincide esattamente con la finestra prima del commit 1f196e8 'fix: quality_gate bot' (2026-07-07), che modifica un gate presente SOLO nel path di esecuzione live (mt5-bot.py) e non nel backtester. È probabile che tale gate (o un residuo dello stesso dopo il fix) stia ancora filtrando i segnali BUY di S00_MFKK, oppure che lo score_mult 0.70 applicato da PerfTracker (già ridotto per underperformance secondo il log self-learning) stia abbassando lo score finale sotto le soglie buy_thr (82-90) definite in signals.py, bloccando sistematicamente l'esecuzione anche quando lo score grezzo supererebbe la soglia. (confidenza 55%, funzione sospetta `quality_gate`). Suggerimento: Confrontare il commit 1f196e8 con la versione precedente per capire esattamente quali condizioni il quality_gate applicava/applica ancora ai segnali BUY di S00_MFKK; verificare se lo score_mult (0.70, da self_learning_log) viene moltiplicato PRIMA del confronto con buy_thr in mt5-bot.py e se questo path è assente nel backtester; loggare in live il valore di b_score sia grezzo che post-score_mult e post-quality_gate per isolare in quale stadio il segnale viene scartato; verificare anche se il fix del 2026-07-07 richiede un riavvio/redeploy del bot non ancora avvenuto. |
| 🤖 AI-Flagged 2026-07-09 | `scripts/mt5-bot.py` | **S05_MFKK_INTRADAY** TRADE_SILENCE: La funzione signal_mfkk_intraday incatena in AND molte condizioni molto restrittive (ADX>=24 + DI spread>=8 + sessione 7-20 UTC + ATR range + allineamento Supertrend H1 + StochRSI assoluto + RSI/momentum/MACD/EMA200), quindi è plausibile che generi segnali rarissimi per design ('ultra-select', target WR>=35% con <80 trade). Tuttavia, dato il precedente noto di gate live-only (quality_gate RSI>75) che ha bloccato i buy per mesi pur con backtest sano, e il commit recentissimo 'fix: quality_gate bot' 2 giorni prima dell'anomalia, è altamente sospetto che in mt5-bot.py esista un ulteriore filtro di esecuzione (quality_gate, soglia ai_score, o flag di rotazione strategia) che impedisce l'invio di ordini per S05 anche quando signal_mfkk_intraday produce 'buy'/'sell', senza equivalente nel backtester. (confidenza 40%, funzione sospetta `quality_gate`). Suggerimento: Confrontare il tasso di segnali generati da signal_mfkk_intraday su dati storici H1 (log diagnostico dei return 'buy'/'sell') con i log di esecuzione live di mt5-bot.py per S05: verificare se il quality_gate, soglie ai_score o flag di enable/rotazione strategia in mt5-bot.py filtrano/scartano i segnali di questa strategia in modo non presente nel backtester; se il conteggio segnali lato signals.py è >0 ma zero ordini live, la causa è nel gate di esecuzione, non nella logica di segnale. |
| 🤖 AI-Flagged 2026-07-09 | `scripts/mt5-bot.py` | **S09_MFKK_SCALPING** TRADE_SILENCE: Il TRADE_SILENCE totale (zero trade mai registrati) è più coerente con un blocco strutturale lato esecuzione live che con la sola rarità del segnale: signal_mfkk_scalping richiede una congiunzione molto stringente (stack EMA fibonacci allineato + prezzo oltre e233 + FVG retest attivo nello stesso bar + RSI momentum + OBV/OBV_EMA + eventuale allineamento ST H1 + ADX>=20 + finestra oraria 6-19), quindi genera segnali rari ma non impossibili nel tempo; il fatto che non ne sia mai passato nemmeno uno in produzione suggerisce, per pattern già visto (RSI>75 gate solo in mt5-bot.py), un ulteriore filtro/quality_gate presente solo nel codice di esecuzione live (introdotto o modificato nel commit 1f196e8 'fix: quality_gate bot') che non è replicato nel backtester e che sta scartando sistematicamente i segnali di questa strategia prima dell'invio ordine. (confidenza 55%, funzione sospetta `quality_gate`). Suggerimento: Confrontare riga per riga i gate applicati in mt5-bot.py (es. filtri ADX/RSI/session/spread/ATR specifici per strategy_id) con quelli presenti nel backtester per S09_MFKK_SCALPING; instrumentare il codice live per loggare ogni volta che signal_mfkk_scalping ritorna 'buy'/'sell' ma il trade non viene eseguito, individuando quale check successivo lo blocca (quality_gate, filtro orario broker vs UTC, controllo margine/spread, o mapping strategy_id errato). |
| 🤖 AI-Flagged 2026-07-09 | `scripts/mt5-bot.py` | **S10_OB_FVG_SCALP** TRADE_SILENCE: Il silenzio di 37 giorni coincide temporalmente con l'introduzione del filtro V4 (2026-06-03, subito dopo l'ultimo trade del 2026-06-01): il combo session filter 8-17 UTC + ADX>=20 + skip ATR spike + Supertrend alignment è estremamente restrittivo e riduce drasticamente la finestra di ingresso valida. È inoltre plausibile — dato il precedente storico di gate live-only (RSI>75) — che il parametro 'hour' passato da mt5-bot.py in live non sia in UTC (es. broker time o local time), disallineando sistematicamente il filtro rispetto a quanto validato nel backtest, che potrebbe usare timestamp già normalizzati in UTC. (confidenza 45%, funzione sospetta `N/A (verificare punto di chiamata a signal_ob_fvg_scalp e calcolo di 'hour')`). Suggerimento: Verificare come mt5-bot.py calcola/passa 'hour' a signal_ob_fvg_scalp in live (fuso orario broker vs UTC) e confrontarlo con come il backtester calcola 'hour' per gli stessi timestamp; loggare per ogni barra M30 il valore di hour, adx, atr/atr_avg e l'esito di ciascun gate per capire quale condizione sta effettivamente azzerando i segnali in produzione. Se confermato mismatch timezone, normalizzare la sorgente di 'hour' a UTC in entrambi i path (live e backtest). |
| 🤖 AI-Flagged 2026-07-09 | `scripts/mt5-bot.py` | **S16_GOLDEN_SQUEEZE** TRADE_SILENCE: Il pattern è coerente con il bug storico già noto: un gate di qualità applicato solo lato esecuzione live (mt5-bot.py) può bloccare i segnali di S16 anche se signals.py e il backtest restano sani. Il commit più recente '1f196e8 fix: quality_gate bot + dati backtest adattativi' è il candidato più probabile per aver introdotto/modificato un filtro di qualità (es. su ADX, volatilità, RSI o score AI) che non esiste nel backtester, combinandosi con i filtri già stringenti di signal_golden_squeeze (ADX≥25, DI spread≥8, OBV 3-bar slope, candle≥0.35×ATR, H4 filter per SELL, session 7-18 UTC) rendendo il setup quasi impossibile da soddisfare in condizioni di mercato reali attuali. (confidenza 55%, funzione sospetta `quality_gate`). Suggerimento: Verificare in mt5-bot.py se esiste una funzione quality_gate (o equivalente) introdotta/modificata nel commit 1f196e8 che applica soglie aggiuntive (RSI, ADX, score AI, spread, volatilità) SOLO lato live per S16_GOLDEN_SQUEEZE, e confrontarla con l'assenza dello stesso gate nel modulo di backtest. Loggare (con livello debug) ogni volta che un segnale 'buy'/'sell' di S16 viene generato da signal_golden_squeeze ma poi scartato dal gate live, per isolare quale condizione specifica blocca l'esecuzione. Contestualmente verificare se il news_block 2h e il session filter combinati riducono la finestra operativa in modo eccessivo rispetto al backtest. |
| 🤖 AI-Flagged 2026-07-09 | `scripts/mt5-bot.py` | **S17_CONVERGENCE_SCALP** TRADE_SILENCE: Nessun trade mai registrato per S17_CONVERGENCE_SCALP suggerisce che il segnale, pur sano in logica pura (V3 con ADX>=22 e BB%B 0.58/0.42 verificato dal backtest con PF 1.710), venga sistematicamente bloccato lato esecuzione live in mt5-bot.py, coerente con il precedente pattern noto del quality_gate RSI>75 che esiste solo in produzione e non nel backtester. È anche plausibile un secondo gate specifico H4 introdotto nel commit 'aggiunto blocco H4 in mt5-bot.py' (2026-04-17) che filtra erroneamente tutti i segnali H4 di questa strategia, o un mapping/whitelist strategie in mt5-bot.py non aggiornato dopo la migrazione da M30 a H4. (confidenza 55%, funzione sospetta `N/A`). Suggerimento: Verificare in mt5-bot.py tutti i gate/filtri applicati dopo la generazione del segnale (quality_gate, blocco H4, whitelist strategie/timeframe) per S17_CONVERGENCE_SCALP, confrontando le condizioni con quelle usate dal backtester; controllare se il blocco H4 aggiunto il 2026-04-17 o la lista strategie abilitate per H4 esclude erroneamente S17 dopo il fix del 2026-07-07 (quality_gate + dati backtest adattativi). |
| ⚠ Open | `api/webhook.js` | `memCache` senza cleanup globale — stale indicators dopo restart Vercel |
| ⚠ Open | `api/webhook.js` | `X-TV-Secret` check opzionale — attivare `TV_WEBHOOK_SECRET` in env Vercel per protezione completa |
| ⚠ Open | `public/app.js` | `onclick` button bindings (~lines 160-172) falliscono silenziosamente se elemento assente; usare `wire()` pattern |
| ⚠ Open | `api/db.js` | `createClient()` Turso senza timeout esplicito — dipende da kill Vercel a 10s. Low risk |
| ✅ Fixed 2026-04-28 | `scripts/mt5-bot.py` | Race condition `has_position_in_direction()`: MT5 non registrava posizione prima del check successivo → doppio SELL aperto simultaneamente. Fix: `_strategy_order_tickets` ora salva `(ticket, direction)`, check in-memory first. `MAX_OPEN_ORDERS` 3→2. |
| ✅ Fixed 2026-04-28 | `scripts/news_guardian.py` | **TypeError silenzioso**: `now_utc` tz-aware - `evt_dt` tz-naive → TypeError catturato dall'outer try-except → News Guardian sempre `paused=False` anche con news HIGH USD attive. Fix: `now_utc.replace(tzinfo=None)` in `check_news_risk()`. Polling ridotto 900s→60s. |
| ✅ Fixed 2026-05-12 | `scripts/mt5-bot.py` | **`has_position_in_direction()` mai usata**: funzione definita il 2026-04-28 ma mai chiamata nei blocchi segnale → due strategie diverse aprivano nella stessa direzione simultaneamente. Fix: guard aggiunto in tutti i 6 blocchi (H1, H1sec, live scan, M15, M30, H4). |
| ✅ Fixed 2026-05-12 | `scripts/mt5-bot.py` | **`STRATEGY_PARAMS` sl_mult divergeva da `risk_guardian.py`**: sl_mult=1.0 per S05/S09/S10/S17/S00 mentre STRATEGY_ATR_PARAMS aveva 1.5 → SL troppo stretto se RiskGuardian offline. Fix: allineati a 1.5 per tutte. |
| ✅ Fixed 2026-07-07 | `scripts/mt5-bot.py` | **`quality_gate()` RSI threshold 75→85**: soglia RSI>75 bloccava TUTTI i buy durante mercato toro oro (XAU/USD bull market 2026). Gold RSI≥75 è normale in trend rialzista prolungato. Alzata a 85 (buy) e 15 (sell). Root cause del no-trade per 2.5 mesi. |
| ✅ Fixed 2026-07-07 | `scripts/mt5-bot.py` | **`quality_gate()` assente nel blocco M30**: il blocco M30 non chiamava quality_gate → segnali con ATR spike e DI spread insufficiente passavano filtro. Fix: quality_gate aggiunto dopo check direction. |
| ✅ Fixed 2026-07-07 | `scripts/mt5-bot.py` | **`quality_gate()` assente nel blocco H4**: identico a M30. Fix: quality_gate aggiunto nel blocco H4. |
| ✅ Fixed 2026-07-07 | `scripts/mt5-bot.py` | **Blocco H4 segnali senza parametri `hour`/`h1_trend`**: segnali H4 chiamati come `fn_h4(I_h4, idx)` senza contesto sessione/trend. Fix: routing specifico per S17 (h1_trend) e S00 (hour, tf='H4'). |
| ✅ Fixed 2026-07-09 | `public/modules/core.js`, `api/report.js` | **Model ID `claude-sonnet-4-20250514` scaduto (deprecato dal 2026-06-15)**: causava warning/errore nell'analisi screenshot (tab Analisi) e nei report AI giornalieri/settimanali/mensili. Fix: aggiornato a `claude-sonnet-5` in entrambi i punti (chiamata `/api/chat` in core.js e chiamata diretta Anthropic in report.js). |

## Performance Bottlenecks (mt5-bot.py) — Risolti

1. **`get_candles()` ogni tick** → Fix: cache 60s (`last_candle_fetch_ts`)
2. **`compute_indicators()` ogni tick** → Fix: `cached_I_h1`, ricalcolo solo su `new_h1_bar`
3. **`_pos_state` crescita illimitata** → Fix: cleanup automatico in `manage_positions()` tramite `open_tickets` set

## Architettura — Note

- Frontend `strategy.js` rileva regime e mostra segnali senza contesto TF: è per guida umana
- Bot MT5 applica strategie su TF specifici decisi da StrategySelector — divergenza intenzionale dal frontend
- **Non aggiungere TF filtering al frontend** senza richiesta esplicita

## Backlog Priorità Alta

- [ ] Fix `onclick` con apostrofi: sostituire `onclick='fn(${JSON.stringify(s)})'` con `data-signal` + `addEventListener`
- [ ] Notifiche push (Service Worker) su nuovo segnale Strategy Engine
- [ ] Filtro news calendar: skip 30min prima/dopo high-impact events
- [x] Backtest adattativi multi-TF 2026-07-07: H1 PF 1.64 +$6087/24m · H4 PF 1.86 +$4941/24m. TF ottimali aggiornati per tutte le strategie.
- [ ] Paper trading 2 settimane prima di live su conto reale con nuovo sistema

## Backlog Priorità Media

- [ ] `weekly_dd_pct` nel bot: calcolo reale da deals history MT5 (ora è hardcoded 0.0)
- [ ] `recent_wr_map` per StrategySelector: calcolo WR ultimi 30 trade per strategia da `mt5-trades.json`
- [ ] Alert bot offline > 5 minuti (email o push)
- [ ] UI: mostrare `active_strategy` e `strategy_confidence` nel tab Strategy Engine (già presenti in `bot_status`)
- [ ] UI mobile: ottimizzare catalog strategie per schermi < 400px

## Backlog Priorità Bassa

- [ ] Export journal in CSV/PDF
- [ ] Integrazione COT data automatica settimanale
- [ ] Fine tuning UI: animazioni transizione tra tab
