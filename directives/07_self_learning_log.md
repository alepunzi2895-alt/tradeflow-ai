# TradeFlow AI — Self-Learning Log

> Aggiungere sempre una riga quando si risolve un bug. Formato: data | bug | causa radice | fix.

| Data | Bug | Causa radice | Fix applicato |
|---|---|---|---|
| 2026-09-02 | **Bot fermo da trade dal 2026-07-10 — deadlock `_strategy_order_tickets` (Fase 3 sprint "performance stabile")** | `count_open_positions()` = `max(mt5_count, len(_strategy_order_tickets))`. Un'entry stale (il `pop` post-chiusura falliva quando `strategy_closed` restava `'N/A'` — ticket non risolto e comment non-`TF-AI`) gonfiava il conteggio per sempre → tutti i blocchi segnale controllano `count_open_positions() >= MAX_OPEN_ORDERS` **prima** del self-heal in `has_open_position_for_strategy()` → deadlock permanente su tutte le strategie. Il fix per-ticket del 2026-07-16 (commit d6c89a9) copre solo il path del `pop`, non i casi `'N/A'`. | Riconciliazione attiva nel sync loop di `run()` (`mt5-bot.py` ~L1613): a ogni sync, ogni entry di `_strategy_order_tickets` il cui ticket non è tra le posizioni MT5 reali (`cur_pos_ids`) viene rimossa — **due-strike** (`_stale_strikes`, set locale a run()): rimozione solo dopo 2 sync consecutivi stale, per non togliere una posizione appena aperta non ancora visibile in MT5 (latenza ~500ms). Idem `_strategy_sl_count`. Guard `positions_data or mt5.positions_get(...) is not None` per non agire se MT5 non risponde. **Serve restart bot su VPS** per svuotare lo stato in-memory attuale. `06_known_issues.md` aggiornato. |
| 2026-09-02 | **S00_MFKK — call-path bot/backtester + verdetto V2 morta, V3 candidata non shippata (Fase 1 sprint, owner subagente A)** | (1) `mt5-bot.py::get_signal()` (~L643) chiamava `signal_mfkk_score(I,i)` nudo → senza `hour` il ramo SELL è strutturalmente impossibile (`is_london_ny`=False → `sell_thr`=999) e la finestra 7-22 UTC bypassata; senza `tf` `buy_di_min`=20 (valore M30) invece di 15 su H1. Stessa classe del bug S16. `strategy-engine-v2.py::run_one()` non passava `tf` a S00 (run_adaptive/rm sì). (2) Baseline V2 (peso ADX 0.80) **confermata morta** col backtester realistico: standalone full PF 0.64, holdout PF 0.47, live-window PF 0.49 ≈ live 0.59; 0/4 fold positivi su ogni TF. Il subagente, minando ~150 combo, ha trovato **eq-weight 0.33/0.33/0.34 + R:R TP2.5/SL2.0 H1-only** che passa `is_promotable` sull'holdout (PF 1.51, +$879, 4/4 fold) e rende positiva la finestra live. | **Fix di correttezza call-path applicati** (neutrali finché S00 è hard-blocked): `get_signal()` ora `fn(I, i, hour=hour, tf='H1')` per S00; `run_one()` passa `tf=tf` a S00. **V3 NON portata in `signals.py`**: è di fatto una strategia diversa selezionata via mining sull'holdout (contaminazione multi-comparison) → non è un edge validato. S00 resta hard-blocked (V2, `data/strategy_overrides.json` invariato). V3 documentata in `backtests/results/opt_s00_2026-09-02.json` come **candidata per paper-test dedicato** (percorso S20: 4-6 sett isolate, gate PF≥1.2/WR≥40%, altrimenti ritiro). |
| 2026-09-02 | **Sprint "performance stabile" — Fase 2: validazione di portafoglio consolidata** | Combinati i vincitori Fase 1 (S16 call-path, S17 SL1.75, S20 sessione, trim `REGIME_PRIORITY_*` — **senza** S00 V3). Verificato con `strategy-engine-v2.py --rm --walkforward` (cost model ON) su H1/M30/H4. | Risultato adaptive+RM (holdout PF prima→dopo · full DD prima→dopo): **H1** 0.95→**1.16** · 5018→**2100** (4/4 fold); **M30** 1.04→**1.19** · 1730→1579; **H4** 1.23→**1.26** · full PF 1.60→1.64. Tutti i TF holdout PF > 1.15, DD ridotto (H1 dimezzato). Il guadagno è da **meno trading** (trim dei drag) + S17 SL + S16 call-path, non da nuovi parametri di segnale. File: `backtests/results/bt_sprint_final_{h1,m30,h4}.json`. |
| 2026-09-02 | **S16_GOLDEN_SQUEEZE — bot e backtester chiamavano `signal_golden_squeeze` con argomenti diversi (bot 76 trade vs backtester 15 sulla finestra live) — Fase 1 sprint "performance stabile", owner subagente B** | Il filtro sessione interno di S16 (`if hour is not None and not (7 <= hour < 18)`) è **inerte se `hour` non viene passato**. Mappa completa dei call-site (tabella in `backtests/results/opt_s16_2026-09-02.json`): il backtester passa sempre `hour` (`run_one`/`walk_forward_report`/`run_adaptive_rm`); il bot lo passava solo nel loop H1 secondario (~L2037) e nei blocchi M30 (~L2419, path morto per S16). **Omettevano `hour`**: `get_signal()`/PLAYBOOK (~L643, `fn(I,i)` nudo) e il path StrategySelector primario (~L1880). Risultato: S16 tradava 24/7 su H1 anche in sessione asiatica (il `SESSION_FILTER` `block_hours 0-8` in mt5-bot.py copre solo i blocchi M15/M30, non l'H1). Test harness (`opt_harness.evaluate`): baseline H1 con `hour` ON → holdout PF 0.691; forzando `hour=None` (comportamento bot) → **holdout PF 0.486, DD ~2×, live PF 0.129** — il comportamento reale che ha fatto i 76 trade **non è profittevole sull'holdout**. Il gap 76-vs-15 è solo in parte spiegato dal filtro (15→23 su H1 senza `hour`); il resto è churn architetturale (RiskGuardian esce presto → ri-entra, run_one tiene 1 posizione fino a TP/SL) — fuori da signals.py. **Sweep parametri** (adx_gate 22-32, di_spread 6-10, candle_mult .25-.45, obv_slope 2-4, filtro H4-countertrend keep/loosen/drop, sessione 7-18/7-22/8-17/none, griglia TP {3.0,3.5,4.0}×SL {1.5,1.75,2.0,2.25}): **nessuna variante robusta**. adx_gate=28 dà holdout PF 1.334 ma è un artefatto da 1 trade (celle adiacenti 26/29 collassano a ~0.55, holdout_n invariato 41→41 tra adx 26 e 27 mentre il PF raddoppia). Droppare il filtro H4 sulle SELL distrugge l'edge full-period (PF 1.428→0.986). L'unico gradiente reale (monotòno, plausibile: SL più largo regge il fill intrabar pessimistico) è TP 3.5→3.0 / SL 2.0→2.25 (holdout PF 0.691→0.883, full 1.428→1.610) — ma è **la stessa modifica già validata standalone e poi rigettata** il 2026-07-16 e 2026-07-17 perché peggiorava `run_adaptive_rm()`, e comunque holdout PF resta <1.0. | **1) Riconciliazione call-path (mt5-bot.py, applicata nel worktree sprint)**: aggiunto `hour` a tutti i call-site S16 — `get_signal()` (~L640, ora `fn(I, i, h1_trend=I['st'][i], hour=hour)`), StrategySelector primario (~L1882, `+hour=hour`), blocco M15 (~L2267, `+hour=bar_dt_m15.hour`), blocco M30 (~L2420, `+h4_trend=_h4t` per coerenza). Ogni loop deriva `hour` dal timestamp della propria barra. **Filtro sessione tenuto a 7-18 UTC** (non allargato: 7-22 sembrava promuovibile ma la sotto-finestra 7-20 è peggiore del baseline → jagged). **2) Nessun cambio di parametri di segnale né TP/SL** — baseline resta la config migliore; l'edge di S16 è 2024→metà-2025, sull'holdout è marginale-negativa a ogni parametrizzazione e diventa marginale-positiva (WF canonico holdout PF 1.057) solo via regime-gating + tier RiskGuardian. **3) Divergenza residua da notare**: il bot passa il Supertrend H4 reale come `h4_trend`, il backtester usa la proxy "EMA200 in salita su 4 barre" — entrambi significano "H4 rialzista richiesto per SELL" ma non sono identici barra-per-barra. Accettata per ora (la proxy è ciò che usano tutti i backtest canonici, leggermente conservativa). Deliverable completi in `backtests/results/opt_s16_2026-09-02.json`. |
| 2026-09-02 | **Sprint "performance stabile" — subagente C: retune S17_CONVERGENCE_SCALP (SL 1.5→1.75×ATR), S10 e S09 invariate** | Con il backtester reso realistico in Fase 0 (cost model + fill pessimistico + entry next-open + walk-forward/holdout via `opt_harness.py`), rivalutate le 3 strategie minori. **S17 (H4)**: baseline standalone full PF 0.90 / holdout PF 0.98 (3/4 fold) — SL 1.5×ATR ancora troppo stretto per il range H4. Sweep adx_gate {19,22,25} × BB%B {0.55/0.45, 0.58/0.42, 0.62/0.38} × atr_spike {2.0,2.2,2.5} × TP/SL: l'unico effetto robusto e monotòno è **SL 1.5→1.75** (ogni combo promuovibile ha sl=1.75; adx/BB%B/spike danno solo rumore marginale). Con SL 1.75 (TP 4.0 e param segnale invariati): full PF 1.38, holdout PF 1.32 (**4/4 fold positivi**), live-window PF 1.15→1.59, `is_promotable`=True su tutti i check. In H4 adaptive+RM (produzione): S17 PF 2.09→2.21 holdout, WR 45.7→50.0, roster H4 totale PF 1.60→1.63. **S10_OB_FVG_SCALP (M30)**: il backtester realistico genera solo ~19 trade full / holdout n=4 in 2+ anni — nessuna config è `is_promotable` (best full PF 1.32 @ session 7-19/adx17/sl1.25 ma holdout n=5). Campione troppo sottile per ritoccare → **invariata, lotto NON scalato**. **S09_MFKK_SCALPING (M30, hard-blocked)**: nessuna config raggiunge holdout PF ≥1.15 con n≥30 E full PF ≥1.10; le config con holdout PF ~1.7 (adx25/sess 9-15) hanno full PF 0.61-0.70 e P&L full −$218…−329 (holdout = coda fortunata). Full PF non supera mai ~0.75 → **resta hard-blocked** (coerente con live PF 0.10). | S17 `sl_atr` 1.5→**1.75** sincronizzato in: `risk_guardian.py::STRATEGY_ATR_PARAMS`, `mt5-bot.py::STRATEGY_PARAMS`, `strategy_selector.py::STRATEGIES_CONFIG` base_params (era 1.1, outlier — anch'esso allineato a 1.75), `strategy-engine-v2.py` (`run_one` + `run_adaptive` + `run_adaptive_rm`, 3 rami). Nessuna modifica a `signals.py` (i param di segnale S17 restano adx≥22 / BB%B 0.58/0.42 / atr_spike 2.2). Nessuna modifica per S10/S09. `check_config_consistency.py` → OK. Sweep completo + numeri in `backtests/results/opt_minors_2026-09-02.json`, script `scripts/opt_minors_sprint.py`. Call-path reconciliation: nessun mismatch bloccante (S17 dichiara `h1_trend` ma non lo usa; `hour` correttamente passato in tutti i path bot H4/M30/selector; il bug pre-2026-07-17 di `hour` posizionale in `h1_trend` è già fixato per S09/S10). |
| 2026-09-02 | **Backtester sovrastimava sistematicamente il PF vs live (S00 bt ~1.6 / live 0.59, S16 bt ~1.5 / live 0.88) — Fase 0 sprint "performance stabile"** | 3 cause additive in `strategy-engine-v2.py`: (1) **nessun cost model** — zero spread/slippage/commissioni (il bot usa `deviation=20`); (2) **fill intrabar ottimistico** — nel loop di uscita `if jh>=tp_p: win` era controllato PRIMA di `if jl<=sl_p`, quindi una barra che tocca sia TP che SL veniva contata come vincita; (3) **entry alla close** della candela di segnale invece del next-bar-open (il bot entra sulla barra successiva). In più: il "PF canonico 1.6" era una **media dominata dai dati 2024–metà 2025** — il walk-forward mostra l'edge decaduto a ~breakeven nel 2026 (S00 holdout PF 0.90, S16 1.06; standalone ancora peggio). | Aggiunti a `strategy-engine-v2.py` (retro-compatibili dietro flag): cost model (`HALF_SPREAD_USD` 0.15 / `SLIP_ENTRY_USD` 0.05 / `SLIP_SL_USD` 0.10 / `COMMISSION_USD` 0, calibrati da nuovo `scripts/calibrate_costs.py` contro `performance_cache.json`), `resolve_intrabar()` con fill pessimistico (barra ambigua → SL), entry al next-bar-open. Nuovo `walk_forward_report()` + flag `--walkforward` (4 fold cronologici + holdout finale 20% intoccabile). Flag di ripristino storico: `--no-costs --optimistic-fill --entry-on-close` (riproduce esatti i numeri pre-fix, verificato: PF 1.241). Nuovo `scripts/opt_harness.py` — fitness condivisa per la sprint subagenti (holdout PF = metrica primaria). Calibrazione: S00 bt PF 0.49 su finestra live ≈ live 0.59 ✓; **S16 diverge ancora — bt genera 15 trade su finestra live vs 76 del bot** perché il bot chiama `signal_golden_squeeze(I, i)` senza `hour` (path `get_signal`/PLAYBOOK, riga ~643 mt5-bot.py) → session filter 7-18 UTC bypassato, S16 trada 24/7. Da riconciliare in Fase 1. Vedi `05_backtest.md`. |
| 2026-09-01 | **Nuovo `scripts/reactivation_check.py` — ri-test mensile delle strategie hard-bloccate, + bug parser scoperto costruendolo** | Richiesta utente: dopo aver bloccato S00_MFKK/S09_MFKK_SCALPING/S18_RANGE_REVERSAL nello stesso turno, "un re-test mensile lo attiverei per vedere se cambia il regime e si possono riattivare". Motivazione tecnica per cui serve un check separato A BACKTEST (non basta il self-learning già presente in `performance_tracker.py`): quel meccanismo ricalcola `score_mult` sul WR rolling dei trade LIVE recenti, ma una strategia hard-bloccata smette di generare nuovi trade live (`quality_gate()` la filtra, fix dello stesso giorno) → non accumula mai gli ≥8 trade (`MIN_TRADES_ADJUST`) per essere rivalutata, resterebbe bloccata per sempre anche se il regime di mercato tornasse favorevole. Costruendo lo script è emerso che `daily_maintenance.py::parse_backtest_results()` era rotto (vedi `06_known_issues.md` 2026-09-01) — fix necessario prima che il nuovo script desse numeri sensati | Nuovo script `scripts/reactivation_check.py`: legge le strategie con `score_mult=0.0` da `strategy_overrides.json`, ri-fetcha dati MT5 + ri-esegue il backtest canonico (`strategy-engine-v2.py --rm`) sul loro TF, confronta WR/PF freschi con `BASELINE_STATS`/`BACKTEST_BASELINES`. Candidata alla riattivazione se WR ratio ≥ `PENALTY_THRESHOLD` (0.70, stessa soglia di `performance_tracker.py`) **e** PF ≥ 1.2. **Advisory-only**: non modifica mai `strategy_overrides.json`, appende solo una riga in `07_self_learning_log.md` (con dedup same-day) — la riattivazione resta una decisione umana. **Primo run reale (2026-09-01)**: S09_MFKK_SCALPING risulta 🔓 candidata (WR 54.3% vs baseline 30.2%, ratio 180%, PF 2.880, n=35 — campione piccolo, da verificare prima di riattivare); S00_MFKK e S18_RANGE_REVERSAL restano sotto soglia. Task Scheduler: `schtasks /create /tn "TradeFlowAI_ReactivationCheck" /tr "python -X utf8 <path>\scripts\reactivation_check.py" /sc monthly /d 1 /st 07:00` (comando nel docstring dello script, non ancora registrato — vedi `04_bot_operations.md`) |
| 2026-09-01 | **Reactivation Check mensile — 1 strategia/e candidata/e alla riattivazione**: S09_MFKK_SCALPING@M30 WR 54.3% (ratio 180% vs baseline) PF 2.880 n=35. Bloccate dal self-learning il 2026-07-16 per: WR 11.8% vs baseline 36.0% (33%) — BLOCCATA. **Advisory-only, nessuna modifica automatica a strategy_overrides.json** — richiede revisione umana prima di sbloccare. | ri-test mensile automatico | reactivation_check |
| 2026-09-01 | **Self-learning hard-block bypassato dai playbook statici + ri-test completo roster + S18_RANGE_REVERSAL bloccata** | Su richiesta utente ("analisi sugli ultimi trade... come migliorare le strategie che hanno preso stop" poi "implementa tutto quello che può migliorare" poi "ritestare e tenere attive solo le strategie con WR>50%", chiarito con l'utente: WR>50% **oppure** PF alto e robusto, non taglio WR rigido — alcune strategie del roster hanno edge asimmetrico). Analisi trade reali (MT5 `history_deals_get`, 45 trade ultimi 45gg, non i JSON locali stale): S16_GOLDEN_SQUEEZE WR70.6%/PF2.14/+267.57 · S00_MFKK WR50.0%/PF0.90/-15.95 (7 SL) · **S18_RANGE_REVERSAL WR14.3%/PF0.07/-197.99 (6 SL su 7, mai un TP)** · S10_OB_FVG_SCALP WR50%/PF0.64/-18.60 (n=4) · S17_CONVERGENCE_SCALP WR66.7%/PF1.60 (n=3). Investigando perché S00_MFKK (hard-bloccata dal 2026-07-16, `score_mult=0.0`) continuasse a tradare: **il bug è che l'override è letto solo da `StrategySelector._score_strategy()`**, mentre la maggioranza dei trade reali passa dai playbook statici `REGIME_MULTI_STRATEGIES`/`get_signal()` in `mt5-bot.py`, che non lo consultano mai — vedi `06_known_issues.md` 2026-09-01 per il dettaglio tecnico. Ri-eseguito il backtest canonico da zero (dati MT5 freschi via `fetch_mt5_history.py`, `strategy-engine-v2.py --rm` su M30/H1/H4): **H1 adattivo+RM** PF1.233 WR38.4% +$7509.6/24m (S00 +4574.5 WR36.7%, S16 +3009.3 WR47.2%) · **M30 adattivo+RM** PF1.178 WR35.1% +$4468.2/24m (S00 +4199.7 WR32.9%, S09 +380 WR54.3% n=35, S10 +248.3 WR56.2% n=16, **S16 -270.8 WR36.6%, S18 -89.0 WR39.2%** — entrambe negative su M30) · **H4 adattivo+RM** PF1.635 WR39.7% +$8974.9/24m (S17 +4181.2 WR45.7%, S00 +3636.6 WR36.3%, S16 +1157.0 WR43.9%) · **S18 standalone**: M30 PF0.629 (-$449.9, WR47.3% n=220), H4 PF0.202 (-$428.6, WR16.7% n=18) — negativo ovunque, sia nel ri-test che nel live | **1) Fix bug**: aggiunta `strategy_selector.is_hard_blocked(strategy_id)` (legge `strategy_overrides.json` ad ogni chiamata), richiamata come primo check in `mt5-bot.py::quality_gate()` — unico punto comune a tutti i 6 loop di ingresso (H1 primario/secondario, M15, M30, H4, trigger manuale), quindi un hard-block ora si applica ovunque, non solo al percorso `StrategySelector` primario H1. **2) S18_RANGE_REVERSAL bloccata**: nuova entry `score_mult=0.0` in `data/strategy_overrides.json` (stesso schema che scrive `performance_tracker.py`, così un futuro run di `auto_apply_adjustments()` la sovrascrive naturalmente con dati freschi) — decisione unanime tra backtest storico, ri-test 2026-09-01 e live. **3) S00_MFKK/S09_MFKK_SCALPING**: hard-block esistenti (2026-07-16) lasciati invariati — ora finalmente effettivi grazie al fix del punto 1, non ri-decisi in questo turno. Nota per investigazione futura: il ri-test mostra S00_MFKK come il maggior contributore P&L in tutti e 3 i TF nel sistema adattivo teorico (regime-gated + RiskGuardian tier), ma il WR live reale (13.3% al momento del blocco, 50% nel campione recente) è molto più debole — ipotesi da verificare: i loop secondari statici (`REGIME_MULTI_STRATEGIES`) sono meno selettivi del regime-gating + tier scoring del backtest `run_adaptive_rm()`, quindi S00 in produzione prende setup di qualità inferiore a quelli simulati. **Roster attivo post-fix**: S16_GOLDEN_SQUEEZE (H1), S17_CONVERGENCE_SCALP (H4), S10_OB_FVG_SCALP (M30/regime), S20_FIB_CONFLUENCE (M5, integrata lo stesso giorno) — S00/S09/S18 bloccate. Vedi `06_known_issues.md` 2026-09-01, `02_strategies.md` |
| 2026-09-01 | **S20_FIB_CONFLUENCE promossa da "live isolata 0.03" a "integrata" — deviazione consapevole dal piano 4-6 settimane (solo 4gg trascorsi)** | Richiesta esplicita utente ("mettila attiva normale e non paper"); chiarito via domanda che si intendeva integrazione piena (sizing RiskGuardian + compounding + cooldown condivisi), non solo un fix di testo UI. L'utente ha poi aggiunto un'eccezione: lotto finale ×2 rispetto al valore RiskGuardian standard | `mt5-bot.py::s20_check_entry`: rimosso `S20_LOT` fisso, sizing ora via `rg.get_order_params()` (strategy_confidence=0.55, ai_score=55 — proxy fissi, nessun AI scorer nativo per il segnale M5) poi `lot × S20_LOT_MULT(=2.0)`; aggiunti check `sl_cooldowns_until`/`sl_cooldown_until`/`MAX_OPEN_ORDERS`/`has_position_in_direction` prima dell'entry; ticket registrato in `_strategy_order_tickets` (necessario per la risoluzione nome strategia sui comment MT5 troncati, stesso pattern del fix 2026-07-xx). Rimossa la guardia `_is_s20_close` nel blocco di sync chiusure → le chiusure S20 ora contano nel cooldown SL globale e per-strategia (rischio noto: 2 SL consecutivi su S20 possono mettere in pausa anche H1/M30, da monitorare). **Lifecycle posizione (SL strutturale, TP1 1R parziale+BE, TP2 2R) lasciata intatta** nel mini-manager proprio — `RiskGuardian.manage_positions` continua a saltare i ticket S20, perché l'edge backtestato (OOS PF 1.72) dipende da questa gestione specifica, non da BE/TS generici. Aggiunta entry `S20_FIB_CONFLUENCE` in `risk_guardian.py::STRATEGY_ATR_PARAMS` (tp_atr=sl_atr=1.0, usata solo per il risk-cap 2% interno a `_calc_lot`, non per TP/SL reali che restano fib-based). UI: rimosso badge "🧪 LIVE 0.03" e testo stale "IN OSSERVAZIONE (paper), il bot non la esegue" in `se-render.js`/`strategy.js` (card ora appare come le altre strategie attive). Resta **fuori da `StrategySelector`/`STRATEGIES_CONFIG`** (nessun supporto M5 nel selector H1/M30/H4 — non re-ingegnerizzato in questo turno). Vedi `04_bot_operations.md` § S20_FIB_CONFLUENCE e `02_strategies.md` |
| 2026-08-28 | PerfTracker S00_MFKK: score_mult 0.00→1.00 ⬆️ | normal | WR 36.7% in range (74% vs baseline) |
| 2026-08-28 | PerfTracker S10_OB_FVG_SCALP: score_mult 1.00→1.30 ⬆️ | outperform | WR 60.0% vs baseline 28.1% (214%) |
| 2026-08-28 | PerfTracker S18_RANGE_REVERSAL: score_mult 1.00→0.50 ⬇️ | streak_penalty | 5 perdite consecutive | WR=36.4% |
| 2026-08-28 | **S20_FIB_CONFLUENCE (port strategia scalping Pine "Repro Overlay — M5") — nessuna configurazione promossa al roster** | Su richiesta utente, portata in `signals.py` la logica di confluenza del Pine: rilevatore estremi a 20 barre + candela di inversione + prezzo oltre Fib 0.382/0.618 (swing 50 barre) + ribbon EMA20/50, trigger sulla transizione, gate R:R ≥ 1.5. **Scelta utente: SL/TP fedeli ai livelli Fibonacci** (BUY: SL=Fib0.236 TP1=Fib0.618 TP2=Fib0.786) invece degli ATR-mult usati dalle altre strategie — aggiunta `sim_fib_confluence()` in `strategy-engine-v2.py` con gestione a parziali (50% TP1 → BE → 50% TP2). **Port fedele**: standalone M5 n=146 WR 17.8% PF 0.51, M15 n=42 WR 9.5% PF 0.49, M30 n=23 WR 17.4% PF 1.06 (n troppo basso); adattivo regime-gated M5/M15/M30 PF 0.79/0.75/0.84. Diagnosi: SL su Fib 0.236 sotto un pullback poco profondo è ~$2–3 su M5 → spazzato dal rumore intracandela prima che TP1 (Fib 0.618) si sviluppi; solo il 10–18% dei trade tocca TP1. **Sprint di tuning** (90 combo × 3 TF, split cronologico 80/20 IS/OOS: `sl_mode` fib vs atr_floor, `tp1` Fib 0.500 vs 0.618, `adx_min` 0/20/25, `fresh_extreme` estremo stretto vs banda 25%+reversal): unico candidato con campione utile positivo su entrambe le finestre = `sl_mode=fib · tp1=0.500 · adx_min=0 · fresh_extreme=False` **solo M5** — IS n=204 WR 26.5% PF 1.156, OOS n=51 WR 27.5% PF 1.29. **Rigettato**: picco isolato sulla griglia (tp1=0.618 → IS PF 0.66; fresh_extreme=True → IS PF 0.71; adx_min=25 → IS PF 0.94 — nessuna cella vicina lo conferma), M15/M30 negativi per quasi ogni combo, edge marginale (+$62/13 mesi su 255 trade → breakeven dopo costi), il bot non ha loop M5 | Nessuna modifica a `mt5-bot.py` / `risk_guardian.py` / `strategy_selector.py` / UI — non cablata nel live. `signal_fib_confluence` + helper lasciati in `signals.py` come riferimento (come `signal_mfkk_intraday` dopo il ritiro S05), `S20_FIB_CONFLUENCE` resta in `STRATS` di `strategy-engine-v2.py` (re-testabile con `--file`, non presente in nessuna `REGIME_PRIORITY_*` → numeri canonici delle altre strategie invariati). Sweep + report completo in `research/s20_fib_confluence/` (RESULTS.md, `scripts/research_s20_fib.py`). Se in futuro si vuole recuperare l'idea: va riformulata con SL più largo dei livelli Fib (l'ancoraggio 0.236 è il collo di bottiglia) e testata solo su M5, ma serve un edge molto meno fragile di quello trovato. **v2 (2026-08-28, `scripts/research_s20_fib_v2.py`)** su richiesta utente ("ho visto un trader live in profitto con questo Pine su M5"): ingresso confermato (2 candele + higher-low di struttura) + SL strutturale con floor ATR (`min(low−0.15ATR, entry−k·ATR)`, non più Fib 0.236) + filtro trend HTF (EMA200 M5) + TP1=1R/TP2=2R parziali. Dati **estesi a 20 mesi** (M5 refresh 2026-08-28, +2.5 mesi mai visti). Aggregato promettente: WR 10–18% → **52–57%**, PF 1.24–1.33, griglia robustezza 36 celle tutte PF>1, walk-forward 3 terzi PF 1.17/1.37/1.21. **Ma la decomposizione lo smonta**: BUY n=60 PF 0.98 (nessun edge lato lungo), SELL n=84 PF 1.42 (tutto il profitto); SELL-only walk-forward T1 (2025-01→07) PF **0.51 −$33 su 7 mesi**, T2 1.90, T3 1.62 — edge concentrato nella finestra del movimento oro 2025-26 = **scommessa di regime, non edge** (stessa diagnosi di C5). M15/M30 estesi: M15 PF 0.85, M30 PF 1.15 n=38. **v3, ultimo tentativo sul lato BUY** (`scripts/research_s20_fib_v3_buy.py`, richiesta utente): trend HTF forte (stack EMA20>50>200) + momentum intatto (RSI/MACD in salita) + TP1 largo 1.5–2R. Miglior config M5 n=51 WR 47% PF **1.56** +$87, griglia robustezza 48 celle tutte PF>1 — ma walk-forward per terzi di calendario: T1 PF 1.80 / T2 1.87 / **T3 (2026-01→08) PF 1.04–1.20 WR 31–36%** su n=11 (breakeven, carattere cambiato). Stesso difetto di tutte le versioni: profitto concentrato nella parte vecchia/centrale, periodo recente debole → regime bet oro 2024-25, non edge. **v4, filtro sessione + circuit breaker** (`scripts/research_s20_fib_v4_session.py`, richiesta utente): breakdown orario mostrava h16 UTC outlier (WR 85% +$137) + lunedì negativo → filtro ore {8,10,14-16}+no-lunedì portava PF a 2.3-3.4. **Ma validazione onesta**: selezionando le ore solo sul primo 60% e testando sull'ultimo 40%, l'OOS col filtro ore è **PF 1.55 vs 1.72 SENZA filtro** — il "filtro orario migliore" era look-ahead. Circuit breaker "stop dopo profitto nel giorno": a ~3 trade/mese **non scatta mai**; alzando la frequenza taglia i vincitori. **Numero onesto finale**: config di principio (v2 combined + ingresso confermato + SL strutturale + EMA200 + **no-lunedì** + sessione piena London+NY) → full-period PF 1.54 n=111, **OOS ultimi 8 mesi PF 1.72 n=54**, walk-forward 1.16/1.23/1.79, BUY+SELL entrambi positivi. Edge **debole ma reale e out-of-sample** (nel range di S00), non regime bet come v2/v3 isolate. Bloccanti live: il bot non ha loop M5, contributo assoluto piccolo, molta selezione parametri → paper trading prima del capitale. **Decisione utente: paper trading.** Creato `scripts/paper_trade_s20.py` — tracker separato che registra cosa farebbe S20 (config di principio) su M5 senza ordini reali, stato in `data/s20_paper_trades.json`, logica segnale = `research_s20_fib_v2.py::sig_at`. Da lanciare periodicamente (scan ~25h, dedup su bar_time + cooldown 2h). Il bot e il roster live restano intatti. Dopo 2-3 mesi: confronto paper vs OOS backtest. **Aggiornamento stessa giornata — decisione utente: LIVE.** S20 messa live sul bot a **lotto fisso 0.03**, completamente **isolata**: blocco M5 dedicato in `mt5-bot.py::run()` (indipendente da regime/playbook), NON in `StrategySelector`, no compounding/RiskGuardian tier, `RiskGuardian.manage_positions` la salta (guardia `'S20' in comment`), le sue chiusure non toccano i cooldown SL condivisi (`_is_s20_close`), max 1 posizione + cooldown 120min. Gestione: entry + SL strutturale + TP hard 2R; a TP1 (1R) chiude 0.02 e sposta SL residuo (0.01) a BE. `signal_fib_confluence` in `signals.py` riscritta dalla V1 (port fedele) alla **V2 config di principio** (ingresso confermato + higher-low/lower-high + EMA200 + no-lunedì, param `weekday`); `fib_confluence_trade_levels` → SL strutturale + 1R/2R. `strategy-engine-v2.py` passa `weekday` nelle branch S20 (backtest riproduce V2: standalone M5 n=128 WR 54.7% PF 1.66). Card tab Strategie → 🧪 LIVE 0.03, bot POSTa i trade reali aggregati (`s20_push_stats`). Split parziale a 0.03 lot testato: 0.02@TP1+0.01runner → PF 1.50, walk-forward 1.14/1.41/1.70. Stato `data/s20_live_state.json`. Per disattivare: `S20_ENABLED=False`. Piano: 4-6 settimane, se traccia il backtest → 0.04-0.05, se PF<1.2 → archiviare. Vedi `04_bot_operations.md` § S20 LIVE TEST. **Ultrareview (2 nit corretti nello stesso commit)**: (1) `sim_fib_confluence` ritornava `None` se TP1 era stato fillato ma il residuo non si risolveva entro il lookahead → il caller scartava il trade incluso il 50% già bookato; ora `return _mk(booked) if filled_tp1 else None` (effetto nullo sui numeri attuali — dopo TP1 lo stop-BE viene sempre toccato entro le ~30h di lookahead M5 — ma corretto in difesa). (2) il branch `else` di dispatch in `run_adaptive()`/`run_adaptive_rm()` passava `hour` posizionalmente (stesso bug corretto in `run_one()` il 2026-07-17) → trap latente se S20 venisse aggiunta a una `REGIME_PRIORITY_*`: ora `fn(ind,i,hour=hour)` |
| 2026-07-17 | **Ricerca nuova strategia XAU/USD con indicatori nativi TradingView — nessun candidato promosso al roster live** | Su richiesta utente ("a tempo perso"), esplorati 5 archetipi con indicatori TradingView non ancora usati dal progetto (Ichimoku, Supertrend nativo, Parabolic SAR, Keltner Channel, Chaikin Money Flow) su OANDA:XAUUSD H4, split IS(80%)/OOS(20%) cronologico 2020-2026. Miglior candidato **C4** (Keltner(20,2×ATR10) breakout + CMF(20) confirm, TP2.5×ATR/SL1.2×ATR): IS 254 trade WR36.2% PF1.123, OOS 65 trade WR49.2% PF1.767 — PF>1 in entrambe le finestre, campione più ampio del gruppo. Scartati sugli altri 4: C1 breakeven full-period; C3 campione OOS troppo piccolo (n=21); **C5 (Parabolic SAR+MACD, il miglior P&L aggregato tra i 5) aveva PF IS 0.982 — perdente sull'80% più vecchio dello storico**, profittabile solo grazie al trend rialzista recente dell'oro ("regime, non edge"). Validazione aggiuntiva di C4 su richiesta esplicita utente prima di un eventuale paper trading ha smontato anche lui: **PF 0.832 su H1 IS** (non generalizza a un altro timeframe), **PF 1.022 (breakeven) in una finestra oro non-trending 2021-2022** (l'edge dipende dal contesto di trend, non dalla logica breakout+money-flow), costi di transazione realistici ($0.35×2/trade) ininfluenti (non è quindi un problema di esecuzione recuperabile) | Nessuna modifica a `signals.py`/`mt5-bot.py`/`risk_guardian.py`/`strategy_selector.py` — nessun candidato integrato nel bot live, per esplicita decisione dell'utente subordinata all'esito dei test ("se i test sono buoni implementalo"), non soddisfatta. Script Pine dei 5 candidati + report comparativo completo in `research/xauusd_candidates/` (RESULTS.md), conservati come riferimento — se in futuro si vuole recuperare l'idea di C4, andrebbe riformulata come filtro di regime (es. gate ADX H4) e non come sistema standalone. **Curva di equità estesa a tutte le 7 strategie** in `SE.strategies` (era assente su S05_MFKK_INTRADAY e S18_RANGE_REVERSAL) — S05 ora mostra `⛔ RITIRATA` in label più i dati standalone reali (PF 0.80) che ne motivano il ritiro |
| 2026-07-17 | **Re-tuning parametri 5 strategie attive — nessun cambiamento adottato** (+ bug secondario in `run_one()`) | Su richiesta utente, sweep IS(80%)/OOS(20%) cronologico su S00/S16/S09/S10/S17: 1-2 parametri di segnale alla volta (ADX gate, DI spread, soglie score S00, candle filter S16, BB%B S17) + sweep TP/SL mult attorno ai valori correnti. **Segnali**: nessuna variante ha battuto il baseline OOS in modo robusto (criterio: ≥2/3 celle della griglia devono migliorare, non un picco isolato) — per S00 diversi parametri (ADX gate, DI spread, sell_thr) risultano addirittura non vincolanti nel range testato (stessi identici trade), segno che le soglie di score (80% peso ADX) sono già il collo di bottiglia dominante. **TP/SL**: 2 varianti sembravano promettenti in isolamento (S00 TP 3.5→3.0×ATR: OOS PF 0.806→0.976; S16 TP 3.5→3.0×ATR: OOS PF 1.375→1.566) ma **non hanno retto la verifica nel contesto reale** — rilanciando `run_adaptive_rm()` (portafoglio multi-strategia regime-gated, quello che conta in produzione) con S16 TP=3.0 il P&L H1 è sceso $8087→$7766 e la DD è peggiorata; con S00 TP=3.0 l'H1 è migliorato (P&L $8087→$8661, DD $4417→$2923) ma M30 e H4 sono entrambi peggiorati (PF 1.294→1.211 e 1.725→1.623). Nessun cambiamento generalizza su tutti i TF in cui la strategia opera — stessa lezione già vista nell'esperimento SL-adattivo S16 del 2026-07-16: un miglioramento isolato in un contesto di test non implica un miglioramento nel sistema reale multi-TF/multi-regime. **Bug secondario trovato mentre si costruiva l'harness di test**: in `run_one()` (`strategy-engine-v2.py`), il branch `else` del routing argomenti chiamava `fn(ind, i, hour)` **posizionalmente** invece di `fn(ind, i, hour=hour)` — per `signal_mfkk_scalping`/`signal_ob_fvg_scalp`/`signal_mfkk_intraday` (terzo parametro posizionale = `h1_trend`), questo passava l'intero valore `hour` (es. 14) dentro `h1_trend`, facendo scattare quasi sempre il filtro di allineamento Supertrend interno (`h1_trend != -1`/`!= 1`, mai vero per un valore-ora) e bloccando la maggioranza dei segnali standalone di S09/S10 in questa funzione (usata solo per le classifiche "Fase 1", non per `run_adaptive`/`run_adaptive_rm`, quindi i numeri canonici pubblicati non erano affetti) | Nessuna modifica a `signals.py`/`risk_guardian.py`/`mt5-bot.py`/`strategy_selector.py` — i parametri attuali (incl. i mult TP/SL corretti il 2026-07-17 mattina) restano la configurazione migliore trovata, confermata su dati indipendenti IS/OOS. Fix del bug di routing argomenti in `run_one()` (dispatch esplicito per nome invece del branch `else` posizionale). **Nuova feature**: `strategy-engine-v2.py` ora salva `equity_curve: [{t, cum_pnl}]` per-trade in `strategies[id]` (run standalone) e `adaptive_rm.by_strategy[id]` (run di portafoglio) nel JSON di output — usata dal frontend per il grafico curva di equità nella sezione Strategie. File aggiornati: `backtests/results/bt_{h1,m30,h4}_2026-07-17.json` (ora con equity_curve), `backtests/results/optimize_active_strategies_2026-07-17.json` (dettaglio sweep) |
| 2026-07-17 | **Backtester (`strategy-engine-v2.py`) testava con SL più stretti del bot live su 4 strategie su 6 — dal 2026-04-30** | Scoperto durante un cross-check indipendente su TradingView Strategy Tester (porting manuale di S00/S16/S17 in Pine per validare i segnali): per impostare TP/SL nei port ho confrontato le 3 tabelle di parametri del progetto e trovato un disallineamento. Il 2026-04-30 lo SL fu alzato da 1.0×ATR a 1.5×ATR per S00/S05/S09/S10/S17 in `risk_guardian.py::STRATEGY_ATR_PARAMS` e `mt5-bot.py::STRATEGY_PARAMS` (commento: "con ATR H1≈12pt un SL di 1×ATR viene spazzato da normali fluttuazioni intracandela"), ma il cambio non fu mai riportato in `strategy-engine-v2.py` (`run_one()`, `run_adaptive()`, `run_adaptive_rm()`), rimasto a SL=1.0×ATR (S00/S09/S17) e SL=1.2×ATR (S10). `check_config_consistency.py` non lo cattura perché confronta solo bot↔risk_guardian (coerenti tra loro), mai contro il backtester. Probabile concausa (non ancora isolata al 100%) del gap "i numeri non tornano" documentato il 2026-07-16 dopo il fix dei 2 bug di riproducibilità | Allineati tutti i moltiplicatori SL nelle 3 funzioni di `strategy-engine-v2.py` a 1.5×ATR (S00/S09/S17, S05 per coerenza) e 1.5×ATR (S10, era 1.2). Rilanciato il backtest canonico `--rm` su M30/H1/H4 (stessi dati 2026-07-07/16): **WR sale ovunque (+6/+8pp, come atteso da SL più larghi = meno stopout)** ma **DD H1 quasi raddoppia** ($2323→$4417) — il rischio reale del sistema H1 era sottostimato nei numeri finora documentati. Tutte e 3 le TF restano nette positive. Nuovi numeri in `02_strategies.md` § "Refresh 2026-07-17" e `05_backtest.md`. File: `backtests/results/bt_{h1,m30,h4}_2026-07-17.json` |
| 2026-07-16 | **S05_MFKK_INTRADAY ritirata dal roster live (era solo H4)** | Sprint di ricerca a 5 esperimenti paralleli (worktree isolati, IS/OOS 80/20): la "portfolio concentration study" ha confermato **out-of-sample** che droppare S05 dal roster H4 (`REGIME_PRIORITY_H4`) migliora PF 2.192→2.657 (+21%) e DD $905→$615 (-32%) a P&L invariato, per un costo di ~20% trade/gg in meno su H4. Su H1 concentrare è invece ininfluente (S00+S16 già 98.7% del volume). S05 non aveva altro slot vivo: H1/M30 già documentati negativi in `strategy_selector.py` (daily_pnl -2.0 su entrambi). Rilanciato il backtest H4 dopo la rimozione: PF 1.64→1.69, +$42.97→+$44.51/gg (n 430→408) su tutto lo storico | Rimossa la entry `S05_MFKK_INTRADAY` da `STRATEGIES_CONFIG` in `strategy_selector.py` (non più selezionabile dal StrategySelector live — verificato che non era comunque presente nel playbook statico di fallback `REGIME_MULTI_STRATEGIES` in mt5-bot.py, quindi nessun'altra modifica necessaria lì). Rimossa da `REGIME_PRIORITY_H4` in `strategy-engine-v2.py` per coerenza col backtester. Codice/funzione segnale (`signal_mfkk_intraday`) lasciati intatti in `signals.py` — solo non più raggiungibile, reversibile riaggiungendo la entry se servisse rivalutarla. `directives/02_strategies.md` aggiornato (tabella "Strategie Attive" + "TF ottimale per strategia") |
| 2026-07-16 | **Sprint di ricerca strategie — esito complessivo (5 esperimenti, vedi riga sopra + bug sotto per i 2 findings con azione)** | 1) **Audit out-of-sample delle 7 strategie live** (invariate): reggono anche nella finestra OOS che include lo shock oro dic'25→giu'26 (+22% picco poi crollo) — nessuna strategia con campione giudicabile è passata da buona-IS a cattiva-OOS, il tuning storico non era overfitting puro (H1 PF OOS 1.269, H4 PF OOS 2.192; escludendo i 2 mesi di shock H1 sale a 1.857). Non giudicabili per campione insufficiente: S09 H1 (n=2), S16 H4 (n=4), S10 H1 (n=0). 2) **Realismo costi transazione** (spread/slippage mai simulati nel backtester): erosione reale ma modesta, ~4-7% di PF con spread realistico XM Global ($0.35-$0.50), un solo cambio di verdetto (M30 OOS, scenario worst-case, PF 1.142→1.089) — non basta a spiegare il gap storico backtest-vs-live di questo progetto, quello resta spiegato dai bug concreti già in questo file. 3) **Nuova strategia multi-timeframe (S19, H4-trend reale + H1 pullback-EMA20)**: rigetto pulito, PF quasi identico IS/OOS (0.792/0.802) — disconferma stabile, non collasso da overfitting; diagnosticato un problema di uscita (winner che si fermano vicino al breakeven), non di ingresso. Non promossa, codice non collegato al roster (resta in branch di ricerca `worktree-agent-a072ab3577d5fa55b`, non su main). 4) **SL adattivo alla volatilità su S16 H1** (TP fisso 3.5×ATR, SL 2.0×ATR→3.0×ATR se percentile volatilità entrata ≥50%, rolling 150 barre): rivalidato con `run_one()` corretto (branch `worktree-agent-a73ec04d7ba8e5177`) — **il finding sopravvive ma è molto più modesto** di quanto sembrasse. Baseline corretta è molto più forte del misurato originale (OOS PF 1.198→1.381 solo correggendo il bug), e la variante resta sopra ma il margine si restringe (OOS PF 1.350→1.417, +2.6% invece del +12.7% riportato inizialmente). Griglia di robustezza 3×3 (finestra 100/150/200 × soglia 0.45/0.50/0.55): solo 5/9 celle battono la baseline OOS, la cella migliore in-sample in realtà *peggiora* la baseline OOS — **sensibile agli iperparametri, non robusto in senso forte**. Non generalizza: su M30 S16 non è nemmeno viabile standalone (PF<1 sia IS che OOS) e la variante peggiora; su H4 il risultato sembra ottimo ma su soli 3 trade OOS (rumore, non evidenza). **Non wired in signals.py/risk_guardian.py** — edge reale ma troppo marginale/fragile per il live senza altra validazione (più storico, o paper trading) | Nessuna azione di codice da questa riga oltre a quanto già in coda separata (S05 sopra, bugfix backtester sotto) — riferimento per chi rilegge la cronologia di questa giornata di ricerca |
| 2026-07-16 | **2 bug in `strategy-engine-v2.py` rendevano il baseline "canonico" 2026-07-07 non riproducibile** — scoperti per side-effect da 2 dei 5 agenti di uno sprint di ricerca strategie parallelo (worktree isolati), verificati riga per riga dall'assistente prima di applicare il fix | **Bug 1**: `run_adaptive()` (Fase 3, numero "Sistema Adattivo" senza `--rm`) non aveva un ramo TP/SL per `S00_MFKK` (~82% del volume H1) nella catena if/elif (righe ~984-998) → cadeva sul fallback fisso `TP_USD=$20`/`SL_USD=$12` invece di ATR×3.5/1.0. `run_adaptive_rm()` aveva già il ramo corretto (stessa catena, ma con `elif used=='S00_MFKK'` presente) — stesso segnale, esito diverso: sul dataset H4 il 13.7% dei trade cambiava vittoria/sconfitta solo per questo. **Bug 2**: `run_one()` (classifiche standalone "Fase 1", usate storicamente per decidere quali strategie tenere/scartare — es. "S05: 0% WR standalone") etichettava **qualsiasi** uscita non-TP come `outcome='loss'` con `pnl=-abs(entry-close_price)`, anche quando il simulatore BE/trailing interno aveva già spostato `curr_sl_dyn` in profondo profitto prima dell'inversione: un trade trailato a +$40 e chiuso a +$38 veniva registrato come una perdita di $38. Prova diretta sugli stessi 201 trade standalone di S16_GOLDEN_SQUEEZE H1 in-sample: prima del fix WR 28.4%/PF 1.141, dopo WR 58.2%/PF 1.563 — stesso identico set di trade. Bug isolato a `run_one()` (non presente in `run_adaptive()`/`run_adaptive_rm()`, che non hanno trailing dinamico) | **Fix 1**: aggiunto `elif used == 'S00_MFKK': tp_d=round(av*3.5,2); sl_d=round(av*1.0,2)` in `run_adaptive()`, allineato a `run_adaptive_rm()`. **Fix 2**: `run_one()` ora calcola `pnl = (close_price-entry) if sig=='buy' else (entry-close_price)` e `outcome = 'win' if pnl>0 else 'loss'` dal movimento di prezzo firmato reale, non dal ramo che ha chiuso il trade. **Rilanciato il backtest canonico** su M5/M15/M30/H1/H4 (`--rm`, stessi dati 2026-07-07): i numeri restano diversi da quelli documentati come "fonte di verità 2026-07-07" anche dopo il fix (H1 PF 1.863→1.412, H4 PF 1.993→1.64) — nessuna modifica a `signals.py` o ai dati risulta tra le due date, quindi il gap residuo non è pienamente spiegato, solo parzialmente attribuibile a questi 2 bug. Nuovi numeri riproducibili in `02_strategies.md` § "Refresh 2026-07-16", vecchia tabella marcata come superata. File: `backtests/results/bt_{m5,m15,m30,h1,h4}_2026-07-16.json` |
| 2026-07-09 | Card "Calendario Eventi" (dashboard) spesso vuota | `api/analysis.js` type=calendar aveva solo 2 URL (stesso dominio nfs.faireconomy.media, thisweek+nextweek) senza retry. Verificato in diretta: cdn-nfs.faireconomy.media DNS morto, forexfactory.com 403, nfs.faireconomy.media/nextweek 404 — nfs.faireconomy.media/thisweek è l'unica fonte viva ma rate-limita (429) su richieste concorrenti/ripetute, senza retry = card vuota permanente fino al prossimo hard-refresh fortunato. Un primo tentativo con 5 URL in Promise.allSettled ha peggiorato la cosa (raffica concorrente sullo stesso host → 429 riprodotto live) | Ridotto a singolo URL primario + retry con backoff 900ms per assorbire i 429 transitori + cache in-memory 5min (pattern da api/webhook.js::memCache, TTL corto ~20s sui risultati vuoti) per non ri-colpire l'endpoint a ogni refresh dashboard |
| 2026-07-09 | **PerformanceTracker cieco per 6 strategie su 7** — self-learning (boost/penalty/hard-block) mai applicato a S05/S09/S10/S16/S17/S18, solo S00_MFKK tracciato | Il broker (XMGlobal-MT5) tronca lato server il campo `comment` degli ordini MT5 a ~16 caratteri totali (`"TF-AI " + 10 char`, es. `TF-AI S16_GOLDEN_SQUEEZE`→`TF-AI S16_GOLDEN`) — mt5-bot.py invia il comment completo, la troncatura avviene fuori dal nostro controllo. `performance_tracker.py::_parse_strategy_from_comment()` faceva match esatto (`sid in BACKTEST_BASELINES`) → ogni trade con comment troncato ritornava None → `update_from_mt5()` lo scartava in silenzio (`if not sid: continue`). Solo S00_MFKK (8 char) è abbastanza corta da non troncare. Scoperto da un log reale del bot condiviso dall'utente: "Posizioni rilevate al riavvio: ['S18_RANGE_']" (nome troncato) + solo una riga PerfTracker nel log (S00_MFKK). Verificato sui 200 trade reali via `mt5_get`: 194/200 risolvibili per prefisso, solo 6 "manual" esclusi correttamente | `_parse_strategy_from_comment()`: se il match esatto fallisce, risolve per prefisso univoco contro gli strategy_id noti (`sid.startswith(raw_sid)`); None (scarta, mai un'attribuzione ambigua) se il prefisso non è univoco. Testato con unit test isolato (13 casi, inclusi tutti gli SID reali troncati + edge case) — tutti passati. **Da deployare sulla VPS**: al prossimo `update_from_mt5()` dopo il pull, i trade degli ultimi 90gg finora scartati vengono recuperati automaticamente (dedup solo su pos_id già in cache) |
| 2026-07-09 | `check_trade_silence()` (daily_maintenance.py, Feature A) falso positivo su tutte le strategie tranne una | Leggeva `mt5-trades.json` locale (scritto solo da `log_trade_to_json()`, un sottoinsieme dei path di esecuzione ordini) — conteneva solo 3 trade con ultimo il 2026-06-04, mentre la history MT5 reale (200 trade, sincronizzata su Vercel ogni ciclo) mostrava l'ultimo trade il 2026-07-07. Ha prodotto 6 righe "🤖 AI-Flagged TRADE_SILENCE" false in known_issues.md (rimosse, vedi riga Fixed) | Riscritto per leggere da `{VERCEL_URL}/api/db` action `mt5_get` (pubblico, non autenticato, alimentato da `get_recent_trades_data()` in mt5-bot.py — la stessa fonte usata dalla dashboard). Verificato: ora restituisce i 200 trade reali con timestamp corretti |
| 2026-07-09 | Memoria coaching (`tradeMemory`/`analysisMemory`) non sincronizzata su Turso | `tradeMemory` (K.mem) salvata solo in localStorage, mai via `dbSaveUserData` — persa cambiando device/browser. `analysisMemory` (K.amem) ripristinata dal cloud in `syncStateFromCloud()` ma mai risalvata dopo un nuovo `saveAnalysisMemory()`/`resetMemory()` — sync a una via sola | Aggiunte 6 chiamate `dbSaveUserData('mem'/'amem', ...)` nei call site di journal.js + branch di restore `doc_type==='mem'` in core.js. Testato in browser: guard anonimo OK, POST corretta se autenticato; round-trip Turso non completabile nel sandbox di sviluppo (rete outbound limitata, non un bug) — round-trip da verificare in ambiente reale |
| 2026-07-09 | `STRATEGY_OPTIMAL_TF` in daily_maintenance.py disallineata da `BASELINE_STATS` per S16/S17 | Dict mai consumato da nessuna funzione nel file originale (dead code) — la migrazione S16 M30→H1 (2026-04-23) e S17 M30→H4 (2026-04-17) era stata applicata a BASELINE_STATS ma non a STRATEGY_OPTIMAL_TF, rimasta indietro senza effetto visibile finché è rimasta inutilizzata. Scoperta dalla AI Root-Cause Review (nuova Feature A) durante il suo primo run reale, diagnosticando un TRADE_SILENCE su S17 | Corretti i due valori (S16→H1, S17→H4) in `STRATEGY_OPTIMAL_TF`, verificati contro directives/02_strategies.md |
| 2026-06-16 | PerfTracker S00_MFKK hard_block (0 trade per 12 giorni) | Cache performance conteneva 20+ perdite anomale Apr-17 (bug pre-MAX_OPEN_ORDERS) che abbassavano WR a 13.3% | Reset performance_cache.json + strategy_overrides.json; regime_playbook.json aggiornato da S05→S16 per TREND |
| 2026-05-15 | PerfTracker S00_MFKK: score_mult 1.00→0.70 ⬇️ | underperform | WR 13.3% vs baseline 49.4% (27%) |
| 2026-04-21 | PerfTracker S00_MFKK: score_mult 0.50→0.70 ⬆️ | recupero parziale | WR 3.3% vs baseline 38.3% (9%) |
| 2026-04-20 | PerfTracker S00_MFKK: score_mult 1.00→0.50 ⬇️ | streak_penalty | 23 perdite consecutive (BUG pre-MAX_OPEN_ORDERS) | WR=23.3% |
| 2026-03-31 | Prezzi XAU non caricavano | Yahoo Finance bloccato + FPMARKETS ticker fallito | Multi-ticker TV Scanner (9 alternative) |
| 2026-03-31 | Indicatori MACD errati (+31 vs -12) | GC=F futures ≠ spot OANDA:XAUUSD | MAI usare GC=F per prezzi live; Yahoo XAUUSD=X come fallback |
| 2026-03-31 | "No candle data" su tutti indicatori | Vercel IP blacklistato da Yahoo Finance | Candele H1 fetched lato browser in mfkk.js |
| 2026-03-31 | ADX = 0,0,0 | `ADX[10]\|60` colonna non valida → null → 0 | Usa `ADX\|60` (default 14) sempre |
| 2026-03-31 | CCI_S mostra "auto" | `step="1"` rifiutava valori decimali | `step="0.01"` su input HTML |
| 2026-03-31 | ADX RMA vs SMA divergenza | Usavamo Wilder RMA, custom TV usa SMA(DX,len) | Server-side calcolo SMA(DX,10) |
| 2026-04-13 | `quality is not defined` | ReferenceError in dashboard.js:565 | Definito qualityBg/Col in updateConfidence() |
| 2026-04-13 | Duplicate strategy logic | strategy.js broken syntax | Unificata logica MT5/Signal, rimosso codice ridondante |
| 2026-04-13 | CORS Calendar Fail | Proxy 3rd party instabili | Creato /api/market server-side proxy per ForexFactory |
| 2026-04-13 | G/S Ratio e Oil bloccati | Ticker mancanti in API unificata | Aggiunti XAG (SILVER) e fallback OIL nel Hub |
| 2026-04-13 | Calendario non caricava | `nfs.faireconomy.media` offline | Multi-source fallback (3 URL FF), slice=60, mapping completo |
| 2026-04-13 | Grafico TradingView schiacciato | Height fissa 600px | Aumentata a 800px container HTML + widget JS |
| 2026-04-14 | MFKK non in sezione Strategies | SE non leggeva dashContext.mfkk | Aggiunti S00_MFKK (score≥75) e S00_MFKK_HWR |
| 2026-04-14 | Confirm dialog bloccava MT5 | `confirm()` bloccante | Rimosso confirm(), aggiunto toast `seToast()` non-bloccante |
| 2026-04-14 | Bottone MT5 sempre abilitato offline | Nessun check stato bot | `seSendTradeToMt5` verifica syncAge<30s prima di inviare |
| 2026-04-14 | Segnali MFKK troppo frequenti | Soglia 68 quasi sempre soddisfatta | Soglia alzata a 75; zona 80-89 ottimale (WR 58.8%) |
| 2026-04-14 | Catalog strategie senza P&L | Mostrava solo PF e WR | Aggiunte colonne P&L 1M/12M/24M e MaxDD |
| 2026-04-14 | Backtest XAUUSD=X delisted Yahoo | HTTP 404 su tutti i chunk | Migrato a GC=F via yfinance per backtesting storico |
| 2026-04-14 | S13/S14 in rotation con P&L negativo | Priority basata su stime | Rimossi dopo backtest 730gg; sostituiti con S04_BB_SQUEEZE in RANGE |
| 2026-04-14 | `ReferenceError: online is not defined` | Variabile `online` usata ma nome corretto è `botOnline` | Rinominato `online` → `botOnline` in strategy.js |
| 2026-04-14 | Ordine MT5 fallisce `retcode=10027` | "AutoTrading disabled by client" | Non è bug — abilitare Algo Trading in MT5 toolbar (vedi 04_bot_operations.md) |
| 2026-04-14 | S00_MFKK_HWR non scattava mai | `seInds.adx` è fallback statico (25) sempre < 35 | Fix: legge `dashContext.mfkk.adx/dip/dim/macd` reali, con fallback a seInds |
| 2026-04-14 | Architettura strategie semplificata | Decisione utente: solo S00_MFKK e S00_MFKK_HWR | Rimosse S01/S04/S06/S09/S12/S13/S14 da UI, SE_STRATEGY_FNS, regimePriority |
| 2026-04-14 | mt5-bot.py aveva vecchie strategie | STRATEGY_PARAMS non sincronizzato con strategy.js | Aggiornati in mt5-bot.py per usare solo S00_MFKK + S00_MFKK_HWR |
| 2026-04-14 | Backtester usava solo yfinance GC=F | Nessun modo per usare dati broker reali MT5 | Creato fetch_mt5_history.py + flag --file in strategy-engine-v2.py |
| 2026-04-14 | Integrazione Ultimate RSI, Momentum, ICT Order Flow | Utente ha richiesto 3 indicatori TV | Tradotti in JS (S02/S03/S04) — logica mantenuta ma non mostrata in UI |
| 2026-04-14 | Refactoring UI Tab Strategie a 2 card | MFKK HighWR rimossa (3 trade in 24m PF 0.83) | Rimossi S01/S02/S03/S04/S00_MFKK_HWR da UI · S05 = V2 Triple MACD H1 |
| 2026-04-14 | Backtest MT5 GOLD — M30 vs H1 verifica | V2 Triple MACD H1 Score=4.149 vs M30 Score=~2.1 | Confermato H1 come TF ottimale per MFKK Intraday |
| 2026-04-14 | UI Strategie: aggiunto trades/day per periodo | Richiesta utente | Aggiunti td_1m/td_6m/td_12m/td_24m in SE.strategies.stats |
| 2026-04-14 | Bot MT5 offline dopo crash silenzioso | Processo zombie impediva nuova connessione MT5 | `taskkill /f /im python.exe` + reconnect automatico |
| 2026-04-14 | Sync Vercel mai aggiornato ("804m fa") | VERCEL_URL terminava con `/` → doppio slash → HTTP 404 | Rimosso slash finale da VERCEL_URL |
| 2026-04-14 | Bot online/offline a tratti in UI | Sync ogni 60s ma soglia UI < 30s → falsi negativi | Sync bot a 20s · soglia UI a 90s (`botOnline = syncAge < 90`) |
| 2026-04-14 | Bot non eseguiva ordini in autonomia | SIGNAL_FNS aveva strategie archiviate, non le attive | Riscritti signal_mfkk_score() e signal_mfkk_intraday() identici a strategy.js |
| 2026-04-14 | Storico trade vuoto in UI | `get_recent_trades_data()` leggeva file mai popolato | Riscritta per usare `mt5.history_deals_get()` direttamente |
| 2026-04-14 | Implementato Risk Manager adattivo | Feature richiesta: AI Score → lot/TP/SL/BE/TS | Creato `scripts/risk_manager.py` · 5 tier risk · integrato in mt5-bot.py |
| 2026-04-15 | Backtest aggiornato su dati MT5 reali (~6m) | `xauusd_h1_mt5.json` copre solo ~6 mesi | Aggiornato §5.2. Per 730gg usare `--mt5` con MT5 aperto |
| 2026-04-16 | Latency in Risk Manager e stats mancanti | `rm.manage_positions` aggiornata solo ogni 1 ora; S10 stats null | Spostata logica nel loop principale (10s); popolato SE.strategies con stats S10 |
| 2026-04-16 | `api/report.js` Anthropic fetch senza timeout | Nessun `fetchT` helper → Vercel hang | Aggiunto `fetchT` helper, timeout 9s |
| 2026-04-16 | `api/price.js` usava `GC=F` come fallback candele | GC=F futures ≠ spot → drift indicatori | Rimosso GC=F; solo XAUUSD=X per XAU |
| 2026-04-16 | Bot MT5 reconnect senza exponential backoff | Wait fisso 5s+30s → spam reconnect | Exponential backoff: 5s → 10s → 20s → ... max 300s |
| 2026-04-16 | CLAUDE.md mancava S10 e S16 | Strategie attive non documentate | Aggiornata sezione Active Strategies con tabella Bot MT5 separata da UI |
| 2026-04-16 | api/webhook.js: memCache senza TTL | Cache stale dopo assenza webhook | CACHE_TTL_MS = 5min + timestamp; fallback su GitHub |
| 2026-04-16 | api/webhook.js: nessuna firma TradingView | Fake segnali possibili | Aggiunta verifica opzionale X-TV-Secret vs env TV_WEBHOOK_SECRET |
| 2026-04-16 | mt5-bot.py: get_candles(300) ogni 10s | 360 chiamate/ora su dati invariati | Cache 60s: riesegue solo se new_ts - last_fetch >= 60 |
| 2026-04-16 | mt5-bot.py: compute_indicators() ogni 10s | CPU waste su dati invariati | cached_I_h1: ricalcolo solo su new_h1_bar |
| 2026-04-16 | strategy-engine-v2.py: S16 TP/SL errato in run_one() | run_one() usava ATR×2.0/1.5; run_adaptive() 3.0/1.2 | Allineato run_one() a 3.0/1.2 come in STRATEGY_PARAMS |
| 2026-04-16 | Backtest combinato Elite 4 + RM su MT5 reale | Prima esecuzione M30 vs H1 | M30 superiore: PF 1.203 vs 1.084; P&L +$6150 vs +$3072; DD $1502 vs $3622 |
| 2026-04-17 | Funzioni segnale duplicate tra mt5-bot.py e strategy-engine-v2.py | Divergenza silenziosa a ogni modifica | Creato scripts/signals.py come source of truth; entrambi i file importano da lì |
| 2026-04-17 | S16_GOLDEN_SQUEEZE in sessione asiatica | Bassa liquidità XAU 00:00-07:59 UTC → WR basso | Aggiunto SESSION_FILTER in mt5-bot.py: skip S16 in ore 0-7 UTC |
| 2026-04-17 | File JSON backtest sparsi nella root (27 file) | Nessuna struttura chiara per risultati vs dati | Creato backtests/results/, backtests/archive/, data/; aggiornati path in tutti gli script |
| 2026-04-17 | Bot sempre offline in UI + posizioni/storico vuoti | sync_to_vercel() gated da `rm and` → mai chiamata se RiskManager fallisce import | Spostata sync nel blocco 20s (indipendente da rm); AI score fetch rimane a 60s senza rm guard |
| 2026-04-17 | Sync Vercel falliva con SSL CERTIFICATE_VERIFY_FAILED | VPS Windows non aveva CA certificates configurati per Python urllib | Aggiunto _SSL_CTX module-level con certifi (fallback: skip verify); fix in sync_to_vercel, fetch_pending_command, RiskManager.fetch_ai_score |
| 2026-04-17 | Nessun feedback loop tra storico trade e strategy selector | WR/PF reali mai usati per aggiustare punteggi | Creato `scripts/performance_tracker.py` — legge deals MT5 per strategy_id (dal commento "TF-AI SID"), calcola WR rolling 30 trade, scrive score_mult in data/strategy_overrides.json, alimenta recent_wr_map in StrategySelector.select() |
| 2026-04-17 | `get_recent_trades_data` mostrava strategia sempre "N/A" | Iterava solo EXIT deal (entry==1); il nome strategia è nel commento dell'ENTRY deal (entry==0) — skippato con `continue` | Riscritto con groupby position_id: abbina ENTRY+EXIT, legge strategy da ENTRY deal, profit da EXIT deals. Rimosso `_deal_to_dict()` helper. |
| 2026-04-17 | Max ordini aperti non limitato | Bot poteva aprire ordini illimitati in parallelo → rischio eccessivo | `MAX_OPEN_ORDERS=3` + `count_open_positions()` guard a ogni call site (H1/M15/M30/secondary H1) |
| 2026-04-17 | Nessuna pausa dopo SL consecutivi | 2 SL di fila non triggeravano alcun cooldown | `consecutive_sl_count` + `sl_cooldown_until`: dopo 2 SL → pausa 1h (`SL_COOLDOWN_H`). Reset a ogni trade in profitto. |
| 2026-04-17 | Ottimizzazione Phase 1: filtri segnale signals.py | S16 obv_rising -$618 P&L; S09 ADX gate rimuoveva FVG validi; S17 ADX gate su M30 -$161 P&L | S05 RSI 54/46 + ADX>=20 hard gate (KEEP); S10 spike ATR >2.5× (KEEP); tutti gli altri filtri revertiti. Baseline M30+RM: PF=1.196, $13.44/gg |
| 2026-04-17 | S17_CONVERGENCE_SCALP migrato da M30 (PF 1.107) a H4 (PF 1.710) | EMA 34/89 crossover troppo rumoroso su M30; H4 ha WR 41.5% vs 25.7% su M30, 94 vs 641 trade | REGIME_MULTI_STRATEGIES: S17 usa H4 (keep M5 per RANGE); aggiunto blocco H4 in mt5-bot.py; TRADE_DURATIONS H4=240min in risk_guardian.py |
| 2026-04-19 | Ottimizzazione Phase 2: filtri segnale signals.py (v4b) | M30 PF 1.185→1.258 (+6.1%), WR 34.1%→37.4%. H4 PF 1.393→1.660 (+19.2%), WR 38.6%→43.1%, DD $1,113→$626 | S16: ADX>=20 + sessione 7-18 UTC + OBV 1-bar slope + candle>=0.20×ATR. S05: StochRSI K>D confluence. S00: sell thr 68→72 + DI spread>=5. S17: ADX>=18 + BB%B 0.55/0.45. S09: INVARIATO (ADX gate rimuoveva FVG validi, WR scendeva da 37.8%→32.5%). Lezione: S16 session filter indirettamente migliora il routing verso S10 (PF 1.796) che compensava. |
| 2026-04-23 | **BUG#1** M30 bloccato da `elif` | Il blocco M30 usava `elif pb_entry.get('tf') != 'M15'` → quando playbook aveva tf=M15, S00/S09/S10/S16 su M30 non giravano mai | Cambiato `elif` → `if` indipendente (come già fatto per H4 a riga 1932) |
| 2026-04-23 | **BUG#2** `RiskManager.fetch_ai_score()` su classe non importata | `current_ai_score = RiskManager.fetch_ai_score(VERCEL_URL)` generava `NameError` se risk_manager.py non importato → catch silenzioso ogni 60s | Aggiunto guard `if RiskManager is not None` + fallback fetch diretto |
| 2026-04-23 | **BUG#3** `stoch_rsi()` periodo stoch=3 invece di 14 | `stoch_rsi(C,14,3,3)` → 3° argomento è `stoch_p` (era 3, deve essere 14) → StochRSI molto più reattivo/noisy vs backtest → WR live divergeva | Corretto in `stoch_rsi(C,14,14,3,3)` |
| 2026-04-23 | **BUG#4** Funzione `bb()` duplicata in mt5-bot.py | Due definizioni `def bb()` a righe 258 e 435 → la seconda sovrascriveva la prima silenziosamente; rischio regressione a ogni refactor | Rimossa prima definizione (L258-264), mantenuta quella con gestione None |
| 2026-04-23 | **BUG#5** Commento TP/SL errato ("`1 punto=$0.01`") | Il commento diceva `TP=$20 → 2000 punti` ma GOLD digits=2 → 1 pt prezzo ≈ $1/0.01lot → tp_use=19.0 ≈ $19 | Corretto commento con formula corretta |
| 2026-04-23 | **DOC** Baseline WR S17 sbagliato in 02_strategies.md | Baseline PerformanceTracker mostrava 25.7%/PF 1.107 (M30 vecchio) → score_mult poteva penalizzare S17 anche se performava bene su H4 | Aggiornato baseline a WR 41.5% / PF 1.710 (H4 ottimale) |
| 2026-04-23 | **DOC** Durata S17 errata in 03_risk_manager.md | Tabella durate stimava S17 su M15/M5 (30/15 min) ma era già migrato a H4 (240 min) | Aggiornata tabella con H4=240min, rimosse righe M15/M5 per S17 |
| 2026-04-23 | **S16 Serie Stop Loss** (PF live crollato a 0.904) | S16 aveva segnali sporchi (Noise OBV/ADX), SL troppo stretto (1.2x) e forte esposizione primaria (1° in TREND) | **Fase A**: Rescaled segnale (ADX 25, DI match, OBV 3-bar, candle 0.35xATR) in `signals.py`<br>**Fase B**: Demotato a 3° scelta nei TREND regimes (sostituito da S05 in H1)<br>**Fase C**: Implementato timeout `STRATEGY_SL_COOLDOWN_H` specifico per singola strategia (2h pausa dopo 2 SL) in `mt5-bot.py`<br>**Fase D**: Aggiunto `quality_gate()` composito pre-trade, aggiustati TP/SL su RiskGuardian.<br>**Fase E**: Bug critico: `sl_cooldowns_until` per-strategia NON controllato nel loop M30 e H4. Fix: aggiunto `continue` per-strategia in entrambi i loop. SL mult S16: 1.5→2.0.<br>**Fase F (backtest 2026-04-23)**: Test sistematico M30/H1/H4 MT5 — S16 su H1 adattivo RM: WR 43.7% +$2370 vs M30 WR 39.7% +$1190. RSI filter testato → no effetto (ADX≥25 già esclude segnali RSI deboli). Decisione: S16 spostato da M30 → H1 come secondario in TREND_UP/TREND_DOWN. Numeri selector aggiornati (PF 1.36 H1 adattivo). |
| 2026-04-28 | **News Guardian silenziosamente disabilitato** | `check_news_risk(now_utc)` riceveva `now_utc` tz-aware (`datetime.now(UTC)`) ma `evt_dt` dal cache era tz-naive → `TypeError: can't subtract offset-naive and offset-aware datetimes` → catturato dall'outer try-except del bot → `current_news_risk` mai aggiornato → sempre `paused=False` nonostante news HIGH USD. Polling ogni 15min aggiungeva blind spot. | Fix: `now_utc.replace(tzinfo=None)` in `check_news_risk()`. Polling ridotto 900s→60s. |
| 2026-04-30 | **NewsGuardian offline — HTTP 429 rate limiting loop** | 3 cause concatenate: (1) API ForexFactory ha rinominato il campo `currency` → `country` in 2026 → filtro falliva silenziosamente → 0 eventi in cache. (2) Con cache vuota `_last_fetch=0` → `refresh()` ritentava ogni 60s → IP bannato (429). (3) Errori fetch a livello DEBUG → problema invisibile nel log. | Fix A: `_fetch_url` usa `e.get('currency') or e.get('country')` per backward compat. Fix B: su fallimento fetch imposta `_last_fetch` con retry delay 30min (invece di 0 → loop 429). Fix C: errori fetch elevati da DEBUG → WARNING. Fix D: aggiunto URL FF alternativo in coda alla lista. |
| 2026-04-30 | **SL frequenti — mismatch ATR H1 vs range H4 + AI Score offline** | 3 cause strutturali: (1) SL 1×ATR H1 (≈12pt) troppo stretto rispetto a range H4 (25-50pt) — spike normali spazzavano stop prima che la direzione si stabilisse. (2) AI Score bloccato a 50.0 perché `fetch_ai_score()` ritornava 50 al fallimento → RiskGuardian sempre NORMAL, nessuna riduzione risk dopo SL. (3) Nessun allargamento SL dinamico nei momenti di alta volatilità. | **Fix A**: `STRATEGY_ATR_PARAMS` in `risk_guardian.py` — SL alzato a 1.5×ATR per S05/S09/S10/S17/S00 (era 1.0×). **Fix B**: `risk_manager.py` — `fetch_ai_score()` ora ritorna `None` al fallimento (non 50.0) per permettere al chiamante di distinguere fetch reale da offline. **Fix C**: `mt5-bot.py` — `_local_ai_score()` calcolata da `consecutive_sl_count`: 0 SL→55, 1 SL→44, 2 SL→32, 3+ SL→20. Usata solo quando Vercel è offline. **Fix D**: `get_order_params()` in `risk_guardian.py` — moltiplicatore ATR spike dinamico: se ATR>1.3×avg → SL×1.3; se ATR>1.6×avg → SL×1.5. |
| 2026-05-08 | **S05 e S10 rimossi dalla rotation su TF non ottimali** | Backtest su dati freschi 24 mesi (2026-05-08): S05 WR 22.7% su M30 TREND e 29.7% su H1 TREND → sotto soglia 35% su entrambi i TF principali. S10 WR 27.1% su H1, P&L -$156 → drag sul sistema H1. Entrambe le strategie sopravvivevano solo grazie al R:R favorevole (TP 3.5×ATR), non per qualità segnale. | Creati `REGIME_PRIORITY_H1` e `REGIME_PRIORITY_M30` separati in `strategy-engine-v2.py` (in precedenza unico dict condiviso). S05 rimosso da TREND M30. S10 e S05 rimossi da TREND/WEAK H1. Slot riallocati a S00_MFKK (WR 52.3%). Risultato: H1 WR 47%→51.6%, PF 1.593→1.863, DD $743→$186 (-75%), mesi+ 21→24/25. M30 WR 42.2%→47.6%, PF 1.441→1.534. |
| 2026-05-12 | **3 bug strutturali causa SL multipli simultanei** | **(1) `STRATEGY_PARAMS` sl_mult divergeva da `STRATEGY_ATR_PARAMS`**: il fix del 2026-04-30 aveva alzato sl_atr a 1.5 in `risk_guardian.py` ma `STRATEGY_PARAMS` in `mt5-bot.py` aveva ancora sl_mult=1.0 per S05/S09/S17/S00. Quando RiskGuardian era offline o falliva, il bot usava sl_mult=1.0 → SL troppo stretto. **(2) `has_position_in_direction()` mai chiamata**: funzione definita il 2026-04-28 ma non usata in nessun blocco di segnale (H1, H1sec, live scan, M30, H4, M15) → due strategie diverse potevano aprire nella stessa direzione simultaneamente → doppia esposizione. **(3) Analisi SL impossibile senza tool dedicato**. | **Fix A**: `STRATEGY_PARAMS` in mt5-bot.py — sl_mult allineati a 1.5 per S05/S09/S10/S17/S00 (invariato S16=2.0). **Fix B**: aggiunto `has_position_in_direction(direction)` guard in tutti i 6 blocchi segnale (H1, H1sec, live scan, M15, M30, H4) — blocca qualsiasi nuovo ordine nella stessa direzione se una posizione già aperta esiste. **Fix C**: creato `scripts/analyze_sl_history.py` — legge MT5 history o performance_cache.json, raggruppa SL per strategia/regime/ora/sessione, mostra heatmap testuale e sequenze. |
| 2026-07-09 | Warning nell'analisi screenshot + report AI | Model ID `claude-sonnet-4-20250514` deprecato/scaduto (retirement 2026-06-15, data corrente 2026-07-09) | Aggiornato a `claude-sonnet-5` in `public/modules/core.js` (funzione `api()`, chiamata `/api/chat`) e `api/report.js` (chiamata diretta Anthropic Messages API) |
| 2026-04-28 | **Serie 6 SL simultanei (S05+S16) — 16:43 UTC** | 3 cause radice:<br>1. **Race condition `has_position_in_direction`**: al momento del check MT5 non aveva ancora registrato la posizione appena aperta (latenza ~500ms) → secondo SELL aperto nella stessa direzione.<br>2. **`_strategy_order_tickets` non memorizzava la direzione** → fallback MT5-only check soggetto a race.<br>3. **Spike news ~16:43 UTC** (+12 pip in <5s) ha spazzato via 6 SL aperti simultaneamente (S16×4 + S05×2). Perdita totale: −$126 in 5 secondi. Circuit breaker non scattato perché le posizioni si sono chiuse tutte contemporaneamente prima che `consecutive_sl_count` potesse bloccare nuovi ordini. | **Fix applicato**:<br>1. `_strategy_order_tickets` cambiato da `{strat: ticket}` a `{strat: (ticket, direction)}` — ora salva la direzione immediatamente al `place_order()` su tutti i TF (H1 primario, H1 secondario, M15, M30, H4).<br>2. `has_position_in_direction()` controlla in-memory PRIMA di MT5 → immune a race condition post-place_order.<br>3. `MAX_OPEN_ORDERS` ridotto da 3 → **2** (max 2 posizioni contemporanee).<br>4. Sync di riavvio aggiornata: salva anche la direzione da `p.type`. |
